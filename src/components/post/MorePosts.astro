---
import type Post from '../../types/post';
import PostPreview from '../post/PostPreview.astro';

interface Props {
    posts: Post[]
}

const {posts} = Astro.props
---

<section>
  <h2 class="mb-8 text-5xl md:text-7xl font-bold tracking-tighter leading-tight">
    All Posts
  </h2>
  <div class="grid grid-cols-1 md:grid-cols-2 md:gap-x-16 lg:gap-x-32 gap-y-20 md:gap-y-32 mb-32">
    {posts.map((post) => (
      <PostPreview
        title={post.title}
        coverImage={post.coverImage}
        date={post.pubDate}
        slug={post.slug}
        excerpt={post.excerpt}
      />
    ))}
  </div>
</section>