---
interface Props {
    dateString: string
}

const {dateString} = Astro.props
const formattedDate = new Date(dateString).toLocaleDateString('en-gb', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
})
---

<time class="italic" datetime={dateString}>{formattedDate}</time>